{
  "packageManager": "pnpm@7.1.5",
  "name": "@masknet/aot-secure-ecmascript",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "publish": "pnpm run build && pnpm run -r build && npx changeset tag && npx changeset publish",
    "build": "tsc -b ./tsconfig.json",
    "build:watch": "tsc -b ./tsconfig.json --watch"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.1",
    "prettier": "^2.7.1",
    "typescript": "4.7.4",
    "vite": "^3.0.4",
    "vitest": "^0.19.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": []
  }
}
